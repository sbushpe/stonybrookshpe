# Everything above plus the tooling that has to pass before a PR merges.
# Install:  .venv/Scripts/python -m pip install -r requirements-dev.txt
-r requirements.txt

httpx2==2.10.0   # test client transport for fastapi.testclient
mypy==2.3.1
pytest==9.1.1
ruff==0.16.3
