"""Persistence: every query and storage call in the backend lives here.

The data layer makes no business decisions and never sees an HTTP object.
Empty until the auth slice lands. See engineering-rules.md §4.2.
"""
